// Ler um valor N e imprimir todos os valores inteiros entre 1 (inclusive) e 
// N (inclusive). Considere que o N será sempre maior que ZERO

let N = parseInt(prompt("Insira o N"));

for(let i = 1; i <= N; i++){
    console.log(i)
}